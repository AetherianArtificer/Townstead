package toughasnails.api.thirst;

/** Test fixture for TAN's optional API; never included in the mod jar. */
public final class ThirstHelper {
    public static boolean enabled;
    public static boolean isThirstEnabled() { return enabled; }
}
